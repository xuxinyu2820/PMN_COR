The folder `PMN_DPGEN` contains the results of the DP-GEN active learning for PMN.

The folder `Production_Model` contains the production model and its training scripts. The dataset is in `PMN_DPGEN`.